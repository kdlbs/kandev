apiVersion: v1
kind: Service
metadata:
  name: kandev
  labels:
    app: kandev
spec:
  type: ClusterIP
  selector:
    app: kandev
  ports:
    - name: http
      port: 8080
      targetPort: backend
      protocol: TCP
