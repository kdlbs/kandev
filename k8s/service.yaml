apiVersion: v1
kind: Service
metadata:
  name: kandev
  labels:
    app: kandev
spec:
  type: ClusterIP
  selector:
    app: kandev
  ports:
    - name: backend
      port: 8080
      targetPort: backend
      protocol: TCP
    - name: web
      port: 3000
      targetPort: web
      protocol: TCP
