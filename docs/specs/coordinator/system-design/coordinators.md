---
id: coordinator-coordinators-design
title: Coordinators in a workspace design
status: draft
system: coordinator
owners:
  - kandev
created: 2026-09-26
last_updated: 2026-09-26
requirements:
  - REQ-COORDINATOR-COORDINATORS-001
  - REQ-COORDINATOR-COORDINATORS-002
  - REQ-COORDINATOR-COORDINATORS-003
  - REQ-COORDINATOR-COORDINATORS-004
  - REQ-COORDINATOR-COORDINATORS-005
  - REQ-COORDINATOR-COORDINATORS-006
---

# Coordinators in a workspace System Design

## Purpose and boundaries

This design covers the `features.coordinator` flag, the `internal/coordinator`
package with its store and CRUD routes, the backend wiring, workspace deletion
and the Coordinators settings tab. The conversation, tool surface, stall
records and proposals reuse the same package and store and are designed in
[copilot](copilot.md), [needs-you](needs-you.md) and [proposals](proposals.md).

Tasks, sessions, agent profiles, executor profiles and workspace scopes are
used through their owners' existing services; this design adds no field to
them.

## Requirement mapping

| Requirement | Design section |
| --- | --- |
| `REQ-COORDINATOR-COORDINATORS-001` | [Flag and wiring](#flag-and-wiring) |
| `REQ-COORDINATOR-COORDINATORS-002` | [Store](#store), [Routes](#routes), [Validation](#validation) |
| `REQ-COORDINATOR-COORDINATORS-003` | [Routes](#routes), [Security](#security) |
| `REQ-COORDINATOR-COORDINATORS-004` | [Settings UI](#settings-ui) |
| `REQ-COORDINATOR-COORDINATORS-005` | [Validation](#validation), [Settings UI](#settings-ui) |
| `REQ-COORDINATOR-COORDINATORS-006` | [Workspace deletion](#workspace-deletion) |

## Flag and wiring

- `features.coordinator` is registered through the typed registry in
  `internal/runtimeflags/registry.go` (environment `KANDEV_FEATURES_COORDINATOR`),
  with `RestartRequired: true`, and in root `profiles.yaml` as `prod: "false"`,
  `dev: "false"`, `e2e: "true"`. The client `features` type gains
  `coordinator`. The `/runtime-feature-flags` checklist and its completeness
  tests apply.
- `internal/backendapp/coordinator.go` wires the package. The store is always
  initialised, as the `persistence/requiredstores` catalog requires of every
  store. When the flag is on at startup it builds the service, records the
  startup pass's start time `T0`, and only then registers the routes,
  subscribes to `task.stalled` and `workspace.deleted` and registers the
  WebSocket forwarder for `coordinator.updated`. It then runs the startup pass
  (stall pruning, proposal recovery, and the conversation cleanup of
  [copilot](copilot.md#conversation-cleanup)) in the background; the routes
  may serve while it runs. The pass is safe against concurrent requests: the
  conversation cleanup considers only tasks created before `T0`, proposal
  recovery is fenced by the claim token ([proposals](proposals.md#recovery)),
  and stall pruning deletes only rows whose task is missing or archived or
  that are older than 30 days. With the flag off none of these is
  built, so every coordinator route is unregistered and returns 404, and no
  subscriber runs. Stored rows are untouched.

## Store

`internal/coordinator/store.go` follows `internal/automation/store.go`:
`CREATE TABLE IF NOT EXISTS`, additive `ALTER`s only, the required migrate
logger, SQLite and PostgreSQL. It is registered in
`persistence/requiredstores/catalog.go`, with an upgrade test from
`storeconformance/testdata/upgrades/v0.93.0/` on both dialects.

`coordinators`:

| Column | Type | Notes |
| --- | --- | --- |
| `id` | text primary key | UUID generated by the service |
| `workspace_id` | text not null | indexed |
| `name` | text not null | trimmed, 1 to 60 characters |
| `agent_profile_id` | text not null | may dangle after a profile is deleted |
| `executor_profile_id` | text not null | |
| `context` | text not null default '' | stored trimmed, at most 4,000 characters |
| `conversation_task_id` | text null | see [copilot](copilot.md) |
| `created_at`, `updated_at` | timestamp not null | UTC |

The index `(workspace_id, created_at, id)` serves the list order. The proposal
and stall tables are defined in [proposals](proposals.md) and
[needs-you](needs-you.md).

## Routes

All routes authorise through the task service's `AuthorizeWorkspaceScope`:
`workspace.read` for reads, `workspace.manage` for writes.

| Route | Result |
| --- | --- |
| `GET /api/v1/workspaces/:id/coordinators` | `{coordinators: [...]}` ordered by `created_at`, then `id` |
| `POST /api/v1/workspaces/:id/coordinators` | 201 with the coordinator |
| `GET /api/v1/workspaces/:id/coordinators/:cid` | the coordinator, plus `agent_profile_status` and `executor_profile_status` (see [Validation](#validation)) |
| `PATCH /api/v1/workspaces/:id/coordinators/:cid` | the updated coordinator; see PATCH body below |
| `DELETE /api/v1/workspaces/:id/coordinators/:cid` | 204 |

A coordinator whose `workspace_id` differs from `:id` is treated as absent
(404). PATCH runs in one transaction that reads the row, validates, and writes
it with `UPDATE ... WHERE id = ? AND workspace_id = ?`. There is no version
column: concurrent edits apply in commit order, and the last committed edit
determines every field it sets.

PATCH body fields are `name`, `agent_profile_id`, `executor_profile_id` and
`context`. A field that is absent is unchanged. A field sent as JSON `null` is
refused with 400 naming the field, for all four fields; clearing the context
is sent as `""`. Unknown fields are ignored.

`name` and `context` are trimmed of leading and trailing Unicode whitespace
before validation and stored trimmed, on create and PATCH. When PATCH carries
`context`, `agent_profile_id` or `executor_profile_id`, the transaction
compares each field's trimmed (for `context`) new value with its stored value;
when any of the three differ it clears `conversation_task_id`, and after
commit the service archives the old conversation task through the task
service's `ArchiveTask`, which stops a running turn. The archived task is kept
until the coordinator or the workspace is deleted
([conversation lifecycle](copilot.md#conversation-lifecycle)). A context that
differs only in leading or trailing whitespace, or a profile id sent back
unchanged, is therefore not a change and keeps the conversation. A failed
archive is logged at warn; the task is already unreferenced, so it resolves to
no coordinator, and the next startup pass archives it.

A profile change takes effect only for the *next* conversation: the running
session was created with the old profile pair, and Kandev does not migrate a
live agentctl session onto a different agent or executor profile mid-session,
so archiving is the only way a changed profile can take effect at all. The
next popover open creates a fresh conversation task and session from the
coordinator's current `agent_profile_id` and `executor_profile_id`
([copilot](copilot.md#conversation-task)), which is also when
`AC-COORDINATOR-COORDINATORS-005.1`/`005.2` recovery is evaluated against the
new profile. This applies equally when the new profile is itself missing or
passthrough: the archive still happens, and the next open reports the new
profile's status rather than silently continuing on the old, now-stale
session.

DELETE runs one transaction deleting the coordinator's proposals and the
coordinator row, then deletes every conversation task of the coordinator,
current and archived, found with `ListCoordinatorOriginTasks`
([copilot](copilot.md#conversation-cleanup)), through the task service, which
stops a running turn as deleting any task does. A failed task delete is logged
at warn; the startup pass deletes the task because its `coordinator_id` names
no coordinator row.
An approval in flight for a deleted proposal finishes its idempotent task
create; its completing update then matches no row, the re-read finds none and
the request returns 404, so the task stays on its board
([proposals](proposals.md#approve)).

## Validation

`internal/coordinator/validate.go` validates create and PATCH:

- name trimmed, 1 to 60 Unicode code points;
- context at most 4,000 code points;
- agent profile exists, belongs to the coordinator's own workspace (a
  non-empty `WorkspaceID` that differs is refused the same as not found), and
  is not CLI-passthrough (the agent settings service's existing passthrough
  flag), else 400 with the field;
- executor profile exists, else 400.

The conversation route and session start repeat both profile checks, so an
agent profile deleted or switched to passthrough after save, or an executor
profile deleted after save, blocks session start and makes the conversation
route return 409.

**Profile status.** One helper, `profileStatus` in
`internal/coordinator/validate.go`, computes both values from the stored ids,
and the GET, the conversation route and session start all use it:

| Field | Values | Meaning |
| --- | --- | --- |
| `agent_profile_status` | `ok`, `missing`, `passthrough` | `missing`: no agent profile with the stored id, or one whose `WorkspaceID` is non-empty and differs from the coordinator's own workspace; `passthrough`: the profile exists, is in-workspace, and its `CLIPassthrough` (`internal/agent/settings/models`) is true; else `ok` |
| `executor_profile_status` | `ok`, `missing` | `missing`: no executor profile with the stored id; else `ok` |

A profile read that fails for any reason other than not found returns 500
from the GET and the conversation route, writes nothing and starts nothing; it
is never reported as `missing`. The coordinator list response does not carry
the statuses.

**409 body.** The conversation route answers a status other than `ok` with
409 and exactly this body, both fields always present:
`{"error": "coordinator_profile_unavailable", "agent_profile_status": ...,
"executor_profile_status": ...}`, using the values above.

**Messages.** The settings page and the copilot map the statuses to copy
through `t()`, one message per status that is not `ok`:

| Status | Message (meaning) | Settings page | Copilot |
| --- | --- | --- | --- |
| agent `missing` | the agent profile was removed; choose another | under Agent profile | first |
| agent `passthrough` | the agent profile uses CLI passthrough, which a coordinator cannot use; choose another | under Agent profile | first |
| executor `missing` | the executor was removed; choose another | under Executor | after the agent message |

The copilot replaces the composer with these messages, stacked in the table's
order, from the statuses of the coordinator GET it already holds, and does not
call the conversation route while either is not `ok`. When the route returns
409 anyway (a profile changed since the GET), the copilot shows the messages
built from the 409 body's two statuses. A status value the client does not
know is shown as the agent or executor `missing` message of its field.

## Workspace deletion

The `workspace.deleted` subscriber runs one transaction deleting the
workspace's coordinators, proposals and stall rows. Conversation tasks,
current and archived, go with the workspace's tasks through the task system. Deleting zero rows is success,
so a redelivered event is harmless.

## Settings UI

- `apps/web/lib/settings/workspace-settings-tabs.ts` adds `"coordinators"` after
  `"secrets"`; `getWorkspaceSettingsTabs(canvasesEnabled, coordinatorEnabled)`
  filters it when the flag is off.
- `apps/web/src/settings-routes.tsx` adds
  `/settings/workspaces/:id/coordinators` and `.../coordinators/:coordinatorId`
  (`new` adds) rendering pages in
  `apps/web/app/settings/workspace/[id]/coordinators/`.
- `apps/web/lib/api/domains/coordinator-api.ts` is the typed client.
- The coordinator page saves through `useSettingsSaveContributor`; delete uses
  the shared confirmation dialog. Fields are disabled for readers using
  `canManageWorkspace` from `useWorkspaceTeamAccess`
  (`apps/web/hooks/domains/workspace/use-workspace-team-access.ts`, derived
  from `SCOPE.workspaceManage` in `apps/web/lib/types/team-access.ts`).
- `lib/settings-discovery/catalog/workspaces.ts` adds the search entry.
- All copy goes through `t()` in the six locales.

## Security

- Every route authorises at the backend; the UI gating is presentation only.
- With `features.auth` off, the local user holds every scope, as elsewhere.
- Coordinator names and context are untrusted text: rendered as text, and the
  context is passed to the agent inside the coordinator's system block
  ([copilot](copilot.md)).

## Observability

The service logs create, edit (with `context_changed`), delete and
workspace-deletion cleanup at info level with workspace and coordinator ids.

## Related decisions

- [Workspace coordinator in core](../../../decisions/2026-09-26-workspace-coordinator.md)
