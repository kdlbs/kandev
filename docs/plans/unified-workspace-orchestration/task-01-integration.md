# Integrate orchestration into existing workspaces

Status: complete. See plan.md for validation evidence and deployment limits.

Use the reviewed platform plan and feature spec. Preserve legacy records, avoid duplicate schedulers, and keep work/personal execution profiles pinned. Verify backend behavior with focused tests and the browser workflow with end-to-end tests before updating the local test instance.

Mobile discoverability follow-up: label the shared workspace settings entry Office and place it immediately after Overview. Retain the existing /agents route and shared horizontal settings tab pattern. Verify the entry is inside the initial phone viewport and opens workspace persona configuration.
