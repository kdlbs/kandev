{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "startup",
  "description": "Allows the bundled startup screen to recover from a typed data ownership conflict.",
  "local": true,
  "remote": {
    "urls": ["http://127.0.0.1:1420"]
  },
  "windows": ["main"],
  "permissions": ["allow-start-temporary-test-instance"]
}
